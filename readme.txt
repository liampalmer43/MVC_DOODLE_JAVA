All the requirements are implemented.

The text files are saved with extension ".txt".
The binary files are saved with extension ".bin" (for binary).
The default saving format is ".txt", applicable for the "All Files" saving selection.

Enhancements:
1.) Forward and reverse animation has been implemented (see "play" and "reverse" button).
2.) An accurate representation of time during "play" and "reverse" events has been implemented.
